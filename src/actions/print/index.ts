export const printMin = (result: number) => {
  console.log(`MIN is : ${result}`);
};
export const printMax = (result: number) => {
  console.log(`MAX is : ${result}`);
};
export const printAverage = (result: number) => {
  console.log(`AVERAGE is : ${result}`);
};
export const printMedian = (result: number) => {
  console.log(`MEDIAN is : ${result}`);
};
export const printAscChain = (result: number[]) => {
  console.log(`ASC chain lenght: ${result.length} and the chain is: ${result}`);
};
export const printDescChain = (result: number[]) => {
  console.log(
    `DESC chain lenght: ${result.length} and the chain is: ${result}`
  );
};
